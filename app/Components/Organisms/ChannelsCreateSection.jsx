import ChannelsCreateAutoplaySection from './ChannelsCreateAutoplaySection';

/** Interactive “Create your own channel” block — autoplay, progress, synced phone UI */
export default function ChannelsCreateSection() {
  return <ChannelsCreateAutoplaySection />;
}
